# IPU PY Treasury System - Environment Configuration Template
# ⚠️  SECURITY WARNING: NEVER commit actual credentials to version control
# Copy this to .env.local for development or .env for production and set actual values

# Database Configuration - Supabase PostgreSQL
# Use the Session Pooler URL for IPv4 compatibility with Vercel
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_DB_URL=postgresql://postgres.your-project:your-password@aws-1-sa-east-1.pooler.supabase.com:5432/postgres
DATABASE_URL=postgresql://postgres.your-project:your-password@aws-1-sa-east-1.pooler.supabase.com:5432/postgres
SUPABASE_ANON_KEY=your-supabase-anon-key-from-dashboard
SUPABASE_SERVICE_KEY=your-supabase-service-role-key-from-dashboard
SUPABASE_ACCESS_TOKEN=your-supabase-access-token
USE_SUPABASE=true

# Authentication - CRITICAL: Generate a strong 64-character secret
# Generate with: openssl rand -base64 48
JWT_SECRET=your-64-character-jwt-secret-MUST-be-strong-and-unique
JWT_EXPIRES_IN=7d

# Admin Configuration (Initial Setup)
ADMIN_EMAIL=admin@your-domain.org
ADMIN_PASSWORD=YourSecurePassword123!

# IPU Paraguay Configuration
RUC_IPUPY=80017726-6
ORGANIZATION_NAME=Iglesia Pentecostal Unida del Paraguay
LEGAL_STATUS=Personería Jurídica N° 17028

# Tax Configuration Paraguay
IVA_RATE_10=0.10
IVA_RATE_5=0.05
FONDO_NACIONAL_PERCENTAGE=0.10

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/tmp/uploads

# Environment
NODE_ENV=development
# Set to 'production' for Vercel deployment

# CORS Configuration - Add your allowed domains, comma-separated
ALLOWED_ORIGINS=http://localhost:3000,https://your-app.vercel.app

# Pagination
DEFAULT_PAGE_SIZE=50
MAX_PAGE_SIZE=200

# Security - Critical for production
BCRYPT_ROUNDS=12
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# Backup Configuration (Optional)
BACKUP_ENABLED=false
BACKUP_FREQUENCY=daily

# Email Configuration (Required for notifications)
SMTP_HOST=your-smtp-host.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
EMAIL_FROM=noreply@your-domain.org
TREASURY_NOTIFICATION_EMAIL=tesoreria@your-domain.org

# External Services (Optional)
SET_API_URL=https://ruc.com.py/api/v1
BANK_API_ENABLED=false

# Development Only
DEBUG_SQL=false
LOG_LEVEL=info

# Application Settings
APP_NAME=IPU PY Tesorería Nacional
APP_VERSION=2.0.0
TIMEZONE=America/Asuncion
CURRENCY=PYG
LOCALE=es_PY

# Google OAuth Configuration (Required for authentication)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_ALLOWED_DOMAINS=your-domain.org.py,your-domain.org
SYSTEM_OWNER_EMAIL=admin@your-domain.org.py

# Security Notes:
# 1. JWT_SECRET must be at least 64 characters for security
# 2. Use environment-specific values, never share between environments
# 3. Rotate secrets regularly (quarterly recommended)
# 4. Use strong passwords for ADMIN_PASSWORD (12+ characters, mixed case, numbers, symbols)
# 5. Restrict GOOGLE_ALLOWED_DOMAINS to your organization's domains only