# IPU PY Treasury System - Environment Variables Template
# Copy this to .env.local and configure with your values

# Convex Backend (Required)
# Get from https://dashboard.convex.dev after creating a project
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Convex Auth - Google OAuth (Required)
# 1. Get Google OAuth credentials: https://console.cloud.google.com/apis/credentials
# 2. Configure in Convex Dashboard: https://dashboard.convex.dev
#    Go to: Auth > Providers > Google OAuth
#    Add your Google Client ID and Client Secret there (not in .env)
# 3. Authorized redirect URI (for Google Cloud Console):
#    - Development: https://<your-deployment>.convex.site/api/auth/callback/google
#    - Production: https://<prod-deployment>.convex.site/api/auth/callback/google
#
# Note: Google OAuth credentials are configured in Convex Dashboard, not environment variables

# Production Site URL (Optional - Vercel auto-sets this)
# For custom domains, set this to your production URL
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Organization Settings
SYSTEM_OWNER_EMAIL=admin@yourdomain.com
ORGANIZATION_NAME=Your Organization Name

# Environment
NODE_ENV=production

# Feature Flags
